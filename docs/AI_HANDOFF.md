# AI handoff

## Durable product state — 2026-09-07

- `data/build-summary.json` is the canonical source for build-scale figures.  Its current terms are: 9,067 rendered lexical entries (all runtime nodes and all searchable entries), made of 9,046 eligible main-map entries and 21 rendered support entries; 9,187 formal relations; 45,095 French definitions; and one connected component.  A lemma with different POS values is more than one lexical entry.
- A1/A2 aligned content words (`NOM`, `VER`, `ADJ`, non-functional `ADV`) are eligible main-map entries.  Closed-class words and functional adverbs remain auxiliary.  This brings common lookup words such as `école`, `maison`, `chat`, `avoir`, and `bonjour` into the runtime.
- The family UI uses only `fam + sourced + derivational_morphology` relations.  The generated audit records 3,077 such edges, 4,652 connected word entries, and 1,644 partial families.  A partial family is not an exhaustive linguistic family.  `semantic_derivation` is shown as an irregular family link, not as a productive suffix rule.
- The old 500-row audit sample changed with the eligibility policy.  `scripts/apply_audit_review.py` carries a decision forward only when its lemma still occurs in the new sample; 153 reviews carry forward and 347 rows remain unreviewed.  Do not describe the new 500-row sample as fully human-reviewed.
- DBnary now uses the registered `official-snapshot-2026-09-01` source snapshot because the old source URL returned 404.  The source hash is pinned in `data/sources.json`.
- Phase 1 learner content is deterministic and contains no AI text: `learner-content.js` projects 4,652 participating lexical entries, 3,077 direct sourced family relations, and 142 observed structure groups.  It is generated from SQLite facts, does not duplicate definitions/examples, and labels observed structure without claiming a productive rule.  `data/learner-content-overrides.json` is intentionally empty and may only add future teaching classifications or notes.

## Verification completed

- `pnpm check:full` — 18/18 build checks passed.
- `pnpm check:runtime`, `pnpm families:check`, six unit tests, `node --check app.js`, and `git diff --check` passed.
- Local browser check confirmed common lookup words, accent-insensitive `ecole`, sourced word-family rendering across prefix/suffix/conversion/irregular-family types, and no mobile horizontal overflow.
- Learner projection build/SQLite validation, static-runtime validation, nine unit tests, and a local side-panel check passed.

## Release status and next work

- GitHub Pages was published from `main` at `15ced88` on 2026-09-07.  Runtime CI and deployment succeeded; the production site was checked with accent-insensitive `ecole` search and the `école` family.  The browser console has only a non-functional missing `favicon.ico` 404.
- Phase 1 deterministic learner content was published from `main` at `18330a2` on 2026-09-08.  Runtime CI and Pages deployment succeeded; production verification confirmed loading, search, the family sidebar, `同结构的其他词族`, and `learner-content.js` on desktop and a 390px mobile viewport.
- Phase 2 pilot remains isolated from runtime and deployment.  `scripts/build_learner_content_pilot.py` deterministically projects the 20 reviewable SQLite fact inputs to `data/learner-content-pilot-input.json`; `data/learner-content-pilot.json` is a separate learner-draft artifact with accurate `actual_model: unknown` provenance.  Durable identity is `stable_lexeme_key + entry_id + sense_id`; sourced family references use Phase 1 stable relation keys, not SQLite IDs.  The validator checks structure, fact references, sourced-example attribution, data-derived aliases/Lexique forms, and input hashes.  It explicitly cannot validate Chinese/meaning/example semantic alignment; reviewers must compare the two artifacts.
- V3 adds `data/learner-content-pilot-senses.json` (all available source senses for the same 20 lexemes) and `data/learner-content-pilot-selection.json` (editorial primary-sense choices).  Learner sense priority is an `ai_draft` judgment with explicit uncertainty, never a sourced frequency fact.  The draft must bind to that exact selected `entry_id + sense_id`; V3 corrected `façon` to its manner sense and `bonté` to its moral-kindness sense.
- The read-only DBnary translation audit is reproducible with `scripts/audit_dbnary_translations.py` against the registered hash-pinned 2026-09-01 core dump.  It found Chinese target codes `zho/cmn/yue/zhb/zhd/zhn`; core translations are entry-linked, not directly sense-linked.  Treat gloss sense-number matches as candidates only, never automatic learner-sense proof.  No enhanced snapshot is registered or local.
- Phase 2B policy hardening: future content must choose a learner-priority `entry_id + sense_id` independently before looking up DBnary candidates.  `sense_mapped_candidate` now explicitly means a *structurally sense-mapped candidate* (a DBnary gloss-number → SQLite-sense join), never semantic approval or a sense-priority signal.  Candidate lookup then undergoes structural and semantic filtering; lexeme-level CEFR may aid discovery but does not make a rare, dated, or specialized sense learner-priority.  The `antenne|NOM` regression (nautical sense structurally mapped while the Chinese candidate is insect-domain) is covered by `scripts/learner_candidate_policy.py` tests.
- Phase 2B pilot is complete on `phase2-learner-pilot`; its 30 externally semantic-reviewed drafts remain isolated from runtime.  It is ready to prepare a separately bounded 100-item milestone under the learner-first selection contract.
- Phase 2C prepares reviewer facts only: `scripts/build_learner_content_100_review_input.py` produces exactly 100 learner-relevant lexemes and four byte-equivalent 25-record review packets.  It selects lexemes and proposes senses from SQLite source-sense metadata before reading translation candidates; every proposal remains `needs_semantic_review`, and no candidate is an approved Chinese gloss.  `scripts/validate_learner_content_100_review_input.py` validates identity, deterministic rebuild, candidate references, dated-sense downranking, the `antenne` mismatch regression, and packet reconstruction without scanning raw DBnary.
- Phase 2C proposal hardening parses only leading parenthetical usage/domain labels: `Vieilli`/`Désuet`/`Archaïque` receive a high penalty, `Littéraire`/`Soutenu`/`Rare` a modest penalty, and recognized leading domains a specialized-sense penalty.  Terms in definition prose or `Note` text never count as markers.  For lowercase common lemmas, the exact lowercase lexical-entry surface wins before source-order tiebreaking; capitalized special entries remain review context.  Compact 4×25 reviewer packets under `data/review/compact/` are deterministic projections of the complete packets.
- Phase 2C learner drafting uses `data/learner-content-100-selection-reviewed.json` as the authoritative external semantic-selection manifest: 99 reviewed primary senses, 18 explicit overrides, and only `travers|NOM` blocked.  `data/learner-content-100-draft.json` remains isolated AI-draft teaching text, never runtime content.  A DBnary candidate remains sourced provenance rather than automatic semantic approval; `antenne|NOM` records the reviewer’s semantic binding because DBnary’s structural sense mapping is stale.
- Enhanced feasibility check: DBnary documents enhancement files as translation-pair → word-sense computed links, but the official directory currently offers French `fr_dbnary_enhancement_20250901.ttl.bz2` (about 1.6 MB), not an enhanced file demonstrably aligned to the project's registered 2026-09-01 core snapshot.  Do not download, register, or mix it with the core dump until a same-snapshot enhanced artifact and its hash/license metadata are available.
- The next content-quality priority is editorial review of the 347 new audit-sample rows and learner-facing Chinese explanations/examples.  Do not turn spelling similarity or AI drafts into sourced word-family claims.
