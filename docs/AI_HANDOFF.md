# AI handoff

## Durable product state — 2026-09-07

- `data/build-summary.json` is the canonical source for build-scale figures.  Its current terms are: 9,067 rendered lexical entries (all runtime nodes and all searchable entries), made of 9,046 eligible main-map entries and 21 rendered support entries; 9,187 formal relations; 45,095 French definitions; and one connected component.  A lemma with different POS values is more than one lexical entry.
- A1/A2 aligned content words (`NOM`, `VER`, `ADJ`, non-functional `ADV`) are eligible main-map entries.  Closed-class words and functional adverbs remain auxiliary.  This brings common lookup words such as `école`, `maison`, `chat`, `avoir`, and `bonjour` into the runtime.
- The family UI uses only `fam + sourced + derivational_morphology` relations.  The generated audit records 3,077 such edges, 4,652 connected word entries, and 1,644 partial families.  A partial family is not an exhaustive linguistic family.  `semantic_derivation` is shown as an irregular family link, not as a productive suffix rule.
- The old 500-row audit sample changed with the eligibility policy.  `scripts/apply_audit_review.py` carries a decision forward only when its lemma still occurs in the new sample; 153 reviews carry forward and 347 rows remain unreviewed.  Do not describe the new 500-row sample as fully human-reviewed.
- DBnary now uses the registered `official-snapshot-2026-09-01` source snapshot because the old source URL returned 404.  The source hash is pinned in `data/sources.json`.
- Phase 1 learner content is deterministic and contains no AI text: `learner-content.js` projects 4,652 participating lexical entries, 3,077 direct sourced family relations, and 142 observed structure groups.  It is generated from SQLite facts, does not duplicate definitions/examples, and labels observed structure without claiming a productive rule.  `data/learner-content-overrides.json` is intentionally empty and may only add future teaching classifications or notes.

## Verification completed

- `pnpm check:full` — 18/18 build checks passed.
- `pnpm check:runtime`, `pnpm families:check`, six unit tests, `node --check app.js`, and `git diff --check` passed.
- Local browser check confirmed common lookup words, accent-insensitive `ecole`, sourced word-family rendering across prefix/suffix/conversion/irregular-family types, and no mobile horizontal overflow.
- Learner projection build/SQLite validation, static-runtime validation, nine unit tests, and a local side-panel check passed.

## Release status and next work

- GitHub Pages was published from `main` at `15ced88` on 2026-09-07.  Runtime CI and deployment succeeded; the production site was checked with accent-insensitive `ecole` search and the `école` family.  The browser console has only a non-functional missing `favicon.ico` 404.
- Phase 1 deterministic learner content was published from `main` at `18330a2` on 2026-09-08.  Runtime CI and Pages deployment succeeded; production verification confirmed loading, search, the family sidebar, `同结构的其他词族`, and `learner-content.js` on desktop and a 390px mobile viewport.
- Phase 2C production integration is prepared on `phase2-main-integration`: `data/learner-sense-content-reviewed.json` is the canonical source for 99 externally semantic-reviewed, exact-sense learner aids; `travers|NOM` is the sole blocked record.  `learner-sense-content.js` is the minimal deterministic runtime projection.  French definitions remain sourced facts; learner Chinese and examples are reviewed learning aids.  The full DBnary candidate layer and prior pilot artifacts remain research-only on `phase2-learner-pilot`.  Future expansion must preserve learner-first sense selection and semantic review.
- Phase 2C was released to `main`; the durable release-record commit is `5cb730d` on 2026-09-09.  Runtime CI, reviewed-learner validation, Pages deployment, and production desktop/390px QA passed; production contains 99 reviewed exact-sense learner aids and keeps `travers|NOM` blocked.
- Phase 3A scale preparation is isolated on `phase3-scale-prep` and is not a runtime or release artifact.  It prepares 500 new learner-sense review records using learner-first, candidate-independent sense proposals plus conservative, explainable risk flags.  The Phase 2C calibration has no dangerous low-risk false negatives, but the initial batch is deliberately not ready for automatic scale-up: only 18/500 are low risk, so a future milestone needs a risk-policy decision before broad learner-content authoring.
- Phase 3B on the same unreleased branch separates that legacy combined risk into independent sense-selection, translation-binding, and content-authoring axes.  Only `sense_selection_tier=high` routes to human sense review; translation/content uncertainty must not promote that route.  Phase 2C's 18 actual sense corrections have no low-tier false negatives, while the content-drift regression set is calibrated separately.  This supports a scalable French-sense routing experiment; learner-content drafting and semantic validation remain a separate future decision.
- Phase 3B also has compact French-only reviewer packets under `data/phase3/sense-review/`: one 34-record low sanity packet, eight medium packets (318 records), and four high packets (148 records).  They preserve the frozen Phase 3A proposals and facts needed for sense adjudication, intentionally exclude Chinese candidates/examples/family data, and remain research-only.
- The same frozen packets have a reviewer-reading projection at `data/phase3/sense-review/all-500-review.jsonl`, with a deterministic 1-based line index and parent-manifest/hash binding.  It is a compact view only, not an adjudication result or a replacement for packet validation.
- External semantic review materialized all 500 Phase 3 French sense decisions: 499 selected (384 accepted proposals, 115 overrides) and only `cesse|NOM` blocked.  The bounded Phase 3C 150-record content draft is research-only and `ai_draft` status: it binds those exact reviewed senses, has no runtime integration, and still requires external content-semantic review.
- The Phase 3C prose source is `data/phase3/learner-content-150-authored-overlay.json`, externally authored record by record.  `build_phase3_content_draft.py` is packaging-only: it must never generate prose from POS or lemma, and it preserves overlay authoring provenance while regenerating the draft and compact review JSONL deterministically.
- The remaining 349 selected Phase 3 senses are prepared as prose-free external-authoring inputs under `data/phase3/content-authoring-349/`; they partition the 499 selected records with the frozen 150 batch and exclude only blocked `cesse|NOM`.  No content for those 349 records has been generated or packaged for runtime.
- Phase 3 final integration remains on `phase3-scale-prep` and is not released: it combines 499 externally semantic-authored exact-sense learner aids with the unchanged 99 reviewed Phase 2C aids.  Final selection applies v1 plus 14 latest-wins v2 corrections; runtime has 598 records, while blocked `cesse|NOM` and Phase 2C `travers|NOM` remain search-only.  Phase 3 UI intentionally does not claim `已审核`.
- The next content-quality priority is editorial review of the 347 new audit-sample rows and learner-facing Chinese explanations/examples.  Do not turn spelling similarity or AI drafts into sourced word-family claims.
