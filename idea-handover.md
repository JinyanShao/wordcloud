# maillage — Idea Handover
> 本文档记录 maillage 的产品定位、数据原则与实现路线，供后续开发（或他人接手）时对齐方向。
> 最后更新：2026-07-27

## 一句话定位

**maillage 是给 B1→C1 法语学习者的“词际关系地图”**——帮学习者把已经认识的词连成网，专治“每个词都认识、但分不清它们之间差别”的中高级瓶颈。

- 不是背单词软件，不与 Anki / Duolingo 竞争记忆流程
- 不是词典，不与法语助手 / Wiktionnaire 竞争释义完整度
- 是“词背过一轮之后，来这里把它们摆对位置”的工具

## 起源与目标形态

最初的想法来自 Obsidian Graph View：语言学习者脑中的词汇不是孤立列表，而是一张网——近义、派生、形近陷阱、语义漂移、程度与语域差异共同构成一片连续地形。传统工具主要教“词 → 释义”，maillage 要显式呈现“词 → 词”。

最终形态不是若干预先命名的“看 / 说 / 离开 / 居住”词群，而是一张**无预设分区、可缩放、可探索的全局词网**：

- 全景时接近 Obsidian Graph View：默认不显示文字，只显示散点、大小、疏密和自然形成的团块
- 缩放或悬停后逐渐出现词形
- 聚焦某词后显示它的官方关系、关系类型和解释
- “词群”是网络结构自然产生的结果，不是建库时预先规定的输入
- 多义词可以成为连接不同区域的桥，而不是被强行塞入单一语义场

**核心原则：先有全局词汇宇宙，再逐步点亮其中的可信关系；不能用少量人工词群冒充全局词网。**

## 2026-07-27 落地快照

阶段 0–2 的第一版已经实际跑通，不再只是方案：

- 从 14,251 个 `lemma + POS` 候选中得到 **7,314 个 eligible 主词**
- 另有 **57 个官方关系支撑词**进入地图，但不计入主覆盖率
- 完成 500 条固定分层人工抽检：361 条同意自动判断，129 条覆盖判断，10 条暂缓
- FLELex / Lexique / CFDICT 在 B1–C1 实词候选上的三方精确覆盖为 62.9%；CFDICT 只覆盖约 64%，不能作为硬门槛
- 生成 21,446 条分信号 `layout_links`，合并为 13,960 条浏览器制图边；完整候选图仍保持 1 个连通分量
- 使用 CEFR–频率径向规则 + Louvain + Barnes–Hut ForceAtlas2 生成 7,371 组固定坐标；只有 DBnary 明示语义关系、Démonette 直接派生和人工审校关系参与角度与局部拓扑，中文释义重叠、Lexique 词形分析、拼写、读音和连通骨架不再左右全局位置
- 已发布 1,675 条 Démonette 词族边和 2,720 条 DBnary 明示语义边；加上 98 条编辑种子并合并重叠来源后，当前共有 **4,472 条唯一官方边**，全部可追溯
- DBnary 为 7,129 / 7,371 个可渲染词提供 7,979 个词条组、31,328 条法语定义；多义词按词条组展示，不再由单条中文提示代表全部意义
- eligible 中 4,393 个词至少有 1 条官方关系，**官方基础覆盖率为 60.1%**；其中 2,054 个词至少有 2 条，官方有效覆盖率为 28.1%
- 前端已改为 Canvas 全局图，不再读取 `CLUSTERS`；全景默认无词名，放大或聚焦后才显词
- 官方关系、尚未审校的制图邻近和个人关系使用三种不同视觉语言
- 当前构建验证以来源哈希、义项主外键、关系冲突和关键词例回归作为质量门，证据见 `data/reports/build-validation.md`

这组数字确认了产品判断：**不需要手工核准几千条基础词典关系，也不需要用 AI 生成全图。结构库与词典可以把第一条、第二条可靠连接铺到 60.1% / 28.1%；人工应集中处理真正需要教学判断的 `compare / drift / trap / cause`，以及剩余高价值孤词。**

### 本轮纠偏：来源分工，而不是由单一数据库主导

- **Démonette 只回答“是不是直接词族”**：跨词性直接派生，以及方向明确、简单前后缀的同词性直接派生；例如 `voir–vision`、`affirmer–affirmation`、`poli–impoli`
- **DBnary / Wiktionnaire 只回答“词典是否明示近义或反义”**：例如 `poli–respectueux`、`poli–impoli`；这类边显示为“来源确认”，不冒充人工完成的细致辨析
- **中文释义只做阅读提示和候选召回**：中文字符重叠不再参与大图位置，也不进入默认发散图，因此不会再次产生 `seau–nager` 一类伪近义
- **拼写与读音只做易混候选**：只有同时形似且音近、并通过高阈值的少量词进入聚焦图，且始终使用虚线和“自动候选”标签
- **同一词对可以有不止一种真实关系**：`poli–impoli` 同时是前缀派生和反义；数据层分别保存，聚焦图优先用反义颜色，详情保留两条证据

## 什么词应该进入 maillage

### 不是“词频前 10k”

直接截取 Lexique 高频词会混入大量冠词、代词、介词和其他封闭类虚词，如 `le / la / les / de`。它们频率极高、在语料和向量空间中连接也很多，但通常不属于 maillage 要解决的“词际辨析”问题。原始 top N 只能作为候选池，不能直接成为产品词表。

maillage 需要先定义一个**有效词汇宇宙（eligible lexicon）**，再衡量官方关系对这个宇宙的覆盖率。

### 三类词表状态

1. **核心词 `eligible`**：进入主星图，也计入官方关系覆盖率
   - 以名词、动词、形容词、实义副词和常用固定表达为主
   - 主要位于 B1–C1，允许保留少量关系价值很高的 A2 / C2 词
   - 有真实使用频率、辨析价值、派生价值或混淆价值
2. **辅助词 `auxiliary`**：只在确有教学价值时出现，不计入主覆盖率
   - 虚词、语法词、极基础词
   - 只有当它参与高价值关系时才展示，例如一词多性、语义漂移或固定搭配
   - 可以作为桥接或解释节点，但不因高频而自动成为大节点
3. **排除词 `excluded`**：不进入主星图
   - 冠词和无独立词汇辨析价值的封闭类词
   - 专名、缩写、乱码、误拼、纯屈折变体
   - 过时、极罕见、领域过窄且与目标用户无关的词
   - 已被另一个 lemma / 词性节点吸收的重复形式

### 建议的圈词流程

1. 用 FLELex / Beacco 提供的 CEFR 分级建立学习者词汇候选池
2. 用 Lexique 4 的频率和语境广度排序，而不是用它决定“是否值得学”
3. 与 CFDICT 等释义资源取交集，补中文释义并排除无法解释的噪声
4. 按词性和封闭类词表过滤虚词、屈折形式、专名与异常项
5. 计算每个候选词的“关系潜力”：能否在派生、近义、对比、形似或语义漂移中形成有价值的边
6. 人工抽检各分数段，确定最终截断线

最终有效规模不预设为 10k。初步估计可能落在 **6,000–8,000 个有效词元/词汇单位**，但必须以第一次词表审计结果为准。

节点底层应尽量使用“lemma + 词性”的词汇单位；屈折形式作为 alias。多义词早期可以共用显示节点，但官方边应能标注它对应的具体义项，避免同一个词的不同义项互相污染。

## 核心资产：官方关系

词典给释义，向量给相似度，但通常不给学习者可直接理解的关系类型和不可互换条件。maillage 的核心资产是**可追溯、经过质量控制、带教学解释的官方边**。

### 关系类型

| 类型 | 含义 | 例 | 生产方式 |
|---|---|---|---|
| `syn` | 近义或局部可换用 | s'effondrer ↔ s'écrouler | 资源召回 + 义项校验 |
| `compare` | 沿某一明确维度的对比 | casser → briser → fracasser | AI 草稿 + 人工编辑 |
| `fam` | 派生且意义大体顺承 | effondrer → effondrement | 结构化资源打底 |
| `drift` | 同族但发生显著语义漂移 | établir → établissement | 算法筛选 + 人工判断 |
| `trap` | 形/音相似但容易误判 | actuellement ≠ actually | 相似度召回 + 人工判断 |
| `ant` | 反义或对立 | augmenter ↔ diminuer | 资源召回 + 义项校验 |
| `cause` | 因果或使役结果 | faire tomber → tomber | 资源线索 + 人工判断 |

原型中的 `axis` 后续应迁移为更通用的 `compare`，并强制保存 `dimension`，因为目前的“轴”已经混合了强度、语域、目的性、控制度、具体/抽象等不同维度。`trap` 也应保存 `subtype`，区分法语内部形近、跨语言假朋友、同形异义等情况。

### 一条可发布官方边的数据结构

```text
a / b                 两端词汇单位
relation              关系类型
dimension / subtype   对比维度或子类型（适用时）
direction             方向（适用时）
label                  图上的短标签
explanation            为什么有此关系、何时不能替换（reviewed 必填）
examples               正例或对比例句（reviewed 必填）
sources                来源及版本
confidence             置信等级
review_status          candidate / sourced / reviewed / rejected
reviewed_at            审核时间
```

### 三个质量层级

1. **候选连接 `candidate`**
   - 向量、共现、编辑距离、LLM 或单一资源自动生成
   - 只进入审核后台或制图层，不能作为产品事实展示
2. **来源确认边 `sourced`**
   - 来源明确、许可允许、结构关系可机械验证
   - 例如 Démonette 内部且两端均属于有效词表的派生关系
   - 可经过规则校验和分层抽检后批量发布，不要求每条都手写长解释或对比例句
   - 仍须显示来源和简短事实标签，不能伪装成已经编辑完成的教学解释
3. **编辑关系 `reviewed`**
   - 涉及义项、语域、强度、漂移、陷阱或不可替换条件
   - AI 可以起草，但必须由人工终审
   - 这是最能体现 maillage 独特价值的高价值内容

原来的“进库必须逐条人工审核”在 6k–8k 规模下不可持续。修订后的纪律是：

> **每条官方边必须可追溯；确定性结构边可以规则发布并抽检，涉及意义判断的边必须人工终审。**

## 图的数据分层

产品视觉上仍然只有“官方词网”和“个人层”两个世界，但官方词网内部需要区分制图连接与正式知识关系。

### 1. 制图连接 `layout_links`

用途是让 6k–8k 个有效词形成连续、自然的全局地形：

- 来源可包括语义近邻、语料共现、派生候选、Lexique 词形分析、拼写/发音相似度
- 只有释义、来源确认的派生和审校信号可以影响全局角度与局部词群；Lexique `MorphoBase`、拼写、发音和纯连通骨架只保留为候选，不影响位置
- 默认不显示，或只显示为极淡背景
- 不进入关系图例，不给用户作语言学承诺
- **不计入官方关系覆盖率**

为了减少巨大中心节点和随机孤岛，可采用“相互 k 近邻 + 最小连通骨架”的思路：局部关系只保留 mutual kNN，再用少量最低代价连接保证候选图连通。但连通骨架不能参与空间聚类，否则会把人为补上的桥误画成学习关系。布局生成后保存稳定坐标，客户端不应每次随机重算整个世界。

### 全局空间语义契约（layout v2）

大图中的每个视觉变量只承担一种含义：

- **半径 = 学习进阶**：A1/A2 支撑词与高频 B1 靠近中心，B2 居中，C1/C2 靠外；同等级内按 FLELex 频率和 Lexique 语境广度排序
- **角度 = 数据驱动词群**：只使用释义、来源确认的派生和审校信号运行 Louvain 社区发现与 ForceAtlas2；Lexique 词形分析、拼写、读音相似不参与角度
- **局部距离 = 词群趋势，不是语言事实**：相近只表示参与制图的可靠信号较多；正式关系仍只能由 `official_edges` 表达
- **点大小 = 学习拓扑中的加权连接度 + 受限频率修正**：不让原始频率或拼写近邻制造超级节点
- **坐标 = 构建时确定并固化**：浏览器只读取结果，不运行模型、不请求 AI，也不消耗 token

等级带轻微重叠且不绘制可见圆环，因此全景仍保持连续星云感，而不是一张考试等级靶纸。无 CEFR 的少量编辑支撑词按已知邻词的加权平均半径放置，不凭空猜测等级。

Lexique `MorphoBase` 会混合直接派生、广义词根分析与过度切分，例如原始数据把 `division` 的 base 标为 `voir`。因此它统一保存为 `morphology` 候选：只在“查看自动候选”中可追溯查看，不进入中心发散图，不计入派生覆盖，也不影响大词网位置。只有独立结构资源确认或人工审校后，关系才可升级为 `derivation` / `official_edges.fam`。

Démonette 2 是当前 `derivation` 的正式来源。导入器按 exact lemma+POS 对齐，发布跨词性的 `simple`、`motiv-sem`，以及无法定向但明确属于 conversion 的 `simple` 直接关系；同词性只放行方向明确、`simple`、构式为前缀或后缀的直接派生。`indirect`、`complexe`、`motiv-form`、`accidentel` 和其他同词性关系继续暂缓。`motiv-sem` 必须显示为“异形词族”，例如 `voir → vision` 显示“异形词族 · -ion”；不能把语义上直接但形式不规则的关系解释成普通拼写规则。

本次官方包包含 388,354 个 lexeme 和 222,118 条有向关系记录；当前 7,371 个可渲染词汇单位中有 7,326 个可 exact 对齐。3,145 条原始可发布记录折叠为 1,675 条唯一边；唯一的方向冲突词对整组暂缓。完整证据、发布口径和质量门见 `data/reports/demonette-coverage-conflicts.md`。

DBnary 是当前义项、近义和反义的正式来源。导入器读取官方法语 OntoLex 快照，只对齐当前词表中的相同词性，导入法语定义、例句以及明示 `synonym / approximateSynonym / antonym`；未对齐目标不会偷偷扩充词表，同一词对同时出现近义与反义时整组暂缓。本轮明确不使用 DBnary 的 `derivedFrom`，避免与 Démonette 职责重叠。完整证据见 `data/reports/dbnary-sense-relations.md`。

### 2. 官方关系 `official_edges`

- 来自 `sourced` 或 `reviewed` 状态
- 可以在聚焦、缩放和详情中展示
- 必须有来源和置信等级
- 只有它们能够贡献官方覆盖率
- maillage 必须靠这一层形成足够覆盖，不能依赖用户补洞

### 3. 个人连接 `personal_links`

个人层描述“我与词的关系”，而不是替官方判断“词与词的客观关系”。建议只提供学习者语言：

- 我会混淆
- 我这样记
- 在同一句中遇到
- 稍后比较
- 我的注记

个人连接默认本地私有、视觉上使用灰色虚线或便签样式，并且：

- 不进入官方图例
- 不影响公共星图的固定位置
- 不计入官方覆盖率和内容质量指标
- 用户添加官方已有词时，不复制节点，只标记为“我的词”并附加个人信息
- 用户添加官方没有的词时，先作为私人悬挂节点或收件箱词条存在

若未来开放“建议官方关系”，它必须是另一条独立流程：个人提交 → 候选队列 → 审核 → 官方发布，不能把个人边直接升级成官方边。

## 官方覆盖率：宏伟目标如何量化

“星图看起来连通”不等于“官方内容已经覆盖”。必须分别衡量制图连通性和正式关系覆盖。

设有效词表为 `E`，只对 `eligible` 节点计算：

| 指标 | 定义 | 宏伟目标 |
|---|---|---|
| 制图孤岛率 | 在 `layout_links` 中无连接的节点比例 | 0% |
| 官方基础覆盖 | 至少有 1 条 `official_edge` 的节点比例 | ≥ 95% |
| 官方有效覆盖 | 至少有 2 条 `official_edge` 的节点比例 | ≥ 80% |
| 高价值教学覆盖 | 至少有 1 条 `reviewed` 近义辨析/对比/漂移/陷阱关系的节点比例 | 全体 ≥ 50%，核心高频 3k ≥ 80% |
| 来源完整率 | 官方边具有可核验来源的比例 | 100% |
| 抽检准确率 | 分层人工抽检判定正确的比例 | 结构边 ≥ 98%，语义边 ≥ 95% |

目标规模以首次词表审计为准，当前可用作容量规划的估算是：

- 6,000–8,000 个 `eligible` 节点
- 12,000–20,000 条官方边，平均每词约 3–5 条
- 其中约 2,000–4,000 条是带明确教学解释的高价值编辑关系
- 另有若干只负责排布的 `layout_links`，数量不计入内容成绩

不要用“总词数”“总边数”作为唯一进度条。真正的进度是：**有效词表中还有多少孤词，以及高频核心词中还有多少没有得到一次真正有价值的解释。**

## 内容生产线

原则：**确定性数据源打底 + 算法批量召回 + AI 在擅长的环节打草稿 + 分层质量控制 + 人工处理意义判断。**

### 1. 建立来源登记表

每个数据源记录：版本、下载日期、用途、许可、署名要求、能否商用、能否再分发、字段映射和更新方式。许可不明确的资源只能用于人工参考，不能直接批量并入产品。

### 2. 生成有效词表

输出的不只是最终列表，还必须有一份可审计报告：

- 每种词性保留/排除数量
- 虚词、专名、屈折形式和异常词数量
- 各 CEFR 等级与频率分布
- 被排除词的原因
- 随机抽检样本

### 3. 生成候选边

- `fam`：Démonette / Morphalou 查表
- `syn` / `ant`：JeuxDeMots 等资源召回，按词性和义项过滤
- `trap`：正字法、音标、公共词缀和跨语言相似度召回；同族词降权
- `drift`：同族词的释义/嵌入差异作为候选信号
- `compare`：在相近词集合内，由 LLM 提议强度、语域、目的性等维度
- `cause`：关系资源和语料模式提供线索

### 4. 候选融合与排序

同一对词可能被多个来源召回。需要合并来源并计算优先级：

```text
审核优先级 ≈ 学习价值 × 词频 × 混淆可能 × 网络覆盖增益 × 多来源支持 ÷ 审核成本
```

优先审核能让孤词获得首条官方边、能连接两个稀疏区域、或涉及高频易混词的候选，而不是按“完成一个语义场”的方式推进。

### 5. 审核工作台

不应直接手改 `data.js`。需要一个本地审核界面，至少支持：

- 查看两端释义、词性、例句、来源和候选信号
- 一键接受、拒绝、改类型、改方向、补维度
- AI 起草短标签、解释和对比例句
- 标记疑难项，避免为了清空队列而误审
- 实时显示哪些高频词仍是官方孤词
- 按关系类型、来源、置信度和覆盖增益抽检

### 6. 发布与回归检查

SQLite 作为中间事实库，导出前自动检查：

- 边的两端是否存在
- 是否重复、自连或方向冲突
- 关系类型必填字段是否完整
- 来源与许可署名是否齐全
- 官方孤词率和度数分布是否异常
- 是否出现由泛词造成的超级中心节点
- 布局坐标是否稳定、全图是否连通

通过后再导出静态前端所需的 nodes / official edges / layout links / positions 文件。

## 数据源清单与注意事项

| 资源 | 用途 | 当前注意事项 | 链接 |
|---|---|---|---|
| FLELex / Beacco | CEFR 学习词表候选 | CC BY-NC-SA 4.0；商业化前需解决许可 | https://cental.uclouvain.be/cefrlex/flelex/download/ |
| Lexique 4 | 频率、语境广度、词形信息 | 用于排序，不等同于 CEFR 词表 | https://www.lexique.org/ |
| Démonette 2 | 派生关系与词族 | CC BY-SA 4.0；适合结构边底座 | https://demonette.fr/ |
| DBnary / Wiktionnaire | 法语义项、明示近义与反义 | CC BY-SA；按词条和义项保存，来源确认不等同于人工辨析 | https://kaiko.getalp.org/about-dbnary/ |
| Morphalou 3 | 形态与词形补充 | 使用前确认具体版本与许可义务 | https://repository.ortolang.fr/api/content/morphalou/2/LISEZ_MOI.html |
| JeuxDeMots | syn / ant / cause 等候选 | 众包关系只作为线索；发布前校验许可和义项 | https://www.jeuxdemots.org/JDM-LEXICALNET-FR/ |
| CRISCO DÉS | 同义与语义空间参考 | 离线演示版有查询限制；无限制使用需联系授权，不能默认免费批量分发 | https://crisco.unicaen.fr/dictionnaire-electronique-des-synonymes-des/version-autonome-du-dictionnaire-electronique-des-synonymes/ |
| CFDICT | 中文释义 | 已有数据；发布时保留 CC BY-SA 署名和兼容性检查 | 现有数据 |

## 视觉与交互策略

“好看”是传播钩子，不是留存理由。它负责让用户愿意停留第一分钟；让人回来的是“原来这两个词不能这样替换”的瞬间。

### 全景层

- 不显示预设语义场名称或边界
- 默认只有散点，文字随缩放逐渐出现
- 中心到外围稳定表达“更常用/更容易 → 更进阶/更低频”；角度表达无名称词群
- 点大小使用受限的综合指标，如频率、加权中心度和高质量关系数；不能直接使用原始词频或固定 k 近邻度数
- 布局由数据计算后固化，不在每次打开时随机漂移
- `layout_links` 最多作为极淡背景，不与官方关系使用同一视觉语言

### 聚焦层

- 点击词后以 `official_edges` 为主体，并按近义、反义、对比、漂移、因果、易混、派生设置分别的显示上限，避免某一种关系淹没全部发散图
- 边显示关系类型、短标签、方向；详情把“来源确认”和“人工审校”分开，并优先展示法语义项
- 未审核候选不得伪装成正式关系；仅允许少量同时形似且音近的候选以虚线进入聚焦图
- 用户个人便签可以叠加，但不改变官方节点位置

### 当前仍未解决的空间问题

预计算坐标、无标签社区、数据分层和渐进显词已经落地。角度质量仍受关系来源覆盖限制：没有可靠释义或派生线索的词只能获得稳定位置，不能假装已经属于可信语义群。下一步提升角度质量，应接入可追溯的词汇关系资源，而不是手调坐标或调用大模型生成位置。

## 实现路线图

时间只作容量估算；每一阶段以验收门槛而不是日期结束。宏伟目标不能直接从原型跳到 8k，必须先证明筛词、召回、审核和发布流水线能够重复运转。

### 阶段 0：数据与许可地基

**当前状态：第一版完成。** 已有来源登记、SQLite schema、可重复导入、哈希和许可检查。FLELex 的非商业限制被保留为明确商业化风险。

**目标：先知道什么可以用、怎么存、如何追溯。**

产出：

- 数据源许可登记表
- SQLite schema：nodes / aliases / sources / edge_candidates / official_edges / layout_links / reviews
- 关系类型规范和每类必填字段
- 可重复运行的导入脚本骨架

验收：任意一条官方边都能追溯到来源、生成规则和审核状态。

### 阶段 1：有效词表审计

**当前状态：第一版完成。** 当前有效规模为 7,314；500 条分层抽检已落库。下一次调整筛选规则时必须重建样本或建立 v2 审计，不能静默覆盖 v1 证据。

**目标：从原始大词表中得到第一版“值得进入 maillage 的词汇宇宙”。**

产出：

- FLELex + Lexique + CFDICT 对齐结果
- `eligible / auxiliary / excluded` 三类词表
- 词性、CEFR、频率和排除原因报告
- 500 个分层随机样本的人工抽检结果

验收：有效词表的误入噪声率可接受，并得到真实规模，而不是提前坚持 8k / 10k / 12k。

### 阶段 2：全局制图原型

**当前状态：第一版完成。** 7,371 节点全图已连通并固化坐标，前端已去掉预设词群。自动邻近中仍存在 CFDICT 误义引出的噪声，因此它们只作为制图候选显示。

**目标：证明有效词表可以形成没有硬编码语义场的稳定星图。**

产出：

- 语义、Lexique 词形、拼写/发音候选连接；来源确认的派生单独入列
- mutual kNN + 连通骨架
- 社区与中心度分析
- 预计算稳定坐标
- 新全景视图：无标签散点、缩放显词、点击聚焦

验收：制图孤岛率为 0；无明显虚词超级中心；刷新后布局稳定；抽查局部邻域大体合理。

### 阶段 3：1k 核心词生产闭环

**目标：不是做一个词群，而是在全图中优先点亮首批高价值区域。**

建议容量：

- 优先级最高的 1,000 个 `eligible` 词
- 官方基础覆盖 ≥ 80%
- 高价值教学覆盖 ≥ 30%
- 建立可用的本地审核工作台

验收：候选生成 → 审核 → 质检 → 导出 → 前端展示可以一键重复；新增一批关系不再需要手改多个文件。

#### 阶段 3 的具体执行顺序

1. **定义核心 1k**：按 FLELex 频率、CEFR、Lexique 语境广度和“当前无官方边”综合排序，不按语义场挑词
2. **先铺确定性结构边**：接入 Démonette 2；与现有 Lexique MorphoBase 交叉验证后，优先发布可追溯的 `fam`，同时识别需要人工判断的 `drift`
3. **再召回语义边**：接入 JeuxDeMots 或许可清晰的替代源，生成 `syn / ant / cause` 候选；中文释义只帮助审核，不作为关系事实来源
4. **形音陷阱单独排队**：由拼写和 IPA 召回 `trap`，同族词降权，避免把普通派生关系误当易混词
5. **做最小审核工作台**：一次只呈现一对词、来源证据、双端释义、候选类型和覆盖增益；支持接受、拒绝、编辑与暂缓
6. **按覆盖增益审核**：先让核心 1k 中更多词获得第一条官方边，再补第二条；不要连续完成某个“词群”
7. **每 200 条发布一次**：重新计算基础覆盖、双边覆盖、关系类型分布、超级中心和分层抽检准确率

阶段 3 首个实际里程碑不是“再做 200 个词”，而是：**让核心 1k 中至少 800 个词拥有一条可追溯官方边，并证明每批 200 条的生产成本和错误率可持续。**

### 阶段 4：3k 核心网络

**目标：形成足以验证产品价值和内容生产成本的核心官方网。**

建议容量：

- 3,000 个核心有效词
- 官方基础覆盖 ≥ 90%
- 官方有效覆盖 ≥ 70%
- 核心词高价值教学覆盖 ≥ 60%

同时邀请真实 B1–C1 学习者测试：他们是否能从任意熟词连续探索、是否会保存或复访某条解释、是否能感知个人层与官方层的区别。

验收：质量、用户价值和单位内容成本三项均成立，才扩大到全量。

### 阶段 5：6k–8k 宏伟目标

**目标：让官方关系而非用户关系覆盖有效词汇宇宙。**

- 官方基础覆盖 ≥ 95%
- 官方有效覆盖 ≥ 80%
- 全体高价值教学覆盖 ≥ 50%
- 核心高频 3k 高价值教学覆盖 ≥ 80%
- 来源完整率 100%
- 按关系和来源分层持续抽检

扩张顺序由“覆盖缺口 + 学习价值”决定，不由预设语义场决定。优先消灭官方孤词，再补第二条关系，最后增加更精细的对比解释。

### 阶段 6：个人层精修与可选社区贡献

个人层在官方网络足够可靠后再精修，避免用功能热闹掩盖内容不足：

- “添加单词”改为“记下这个词”
- 使用个人学习关系，而非官方关系类型
- 官方已有词不重复建节点
- 可选导入/导出个人数据
- 若验证有价值，再增加独立的“建议官方关系”审核流程

## 当前技术现状

- 浏览器仍是静态前端，但运行数据改为生成的 `graph-data.js`；不需要在客户端安装图算法
- SQLite `data/processed/maillage.sqlite` 是事实库，保存来源、词汇、候选边、官方边、制图边、审核和稳定坐标
- `data.js` 只保留为历史原型与 98 条编辑种子的来源，不再被前端加载；`CLUSTERS` 也不再参与布局
- Canvas 一次绘制 7,371 个节点；全景使用 4,437 组来源确认或审校词对决定词群角度，缩放显词、搜索和点击聚焦已经可用
- 4,472 条唯一 `official_edges` 使用正式关系样式：DBnary 提供 2,720 条语义来源，Démonette 提供 1,675 条派生来源，98 条编辑种子为人工审校层；重叠边合并来源
- 详情页加载 31,328 条法语定义并按 DBnary 词条组呈现；中文释义明确标为“可能不完整”
- 个人层使用 localStorage `maillage.personal.v2`，以琥珀色虚线叠加，不改变公共固定坐标，也不使用官方关系类型
- 数据构建依赖 Python/pandas；布局构建依赖 Graphology、Louvain 和 ForceAtlas2，成品浏览器无运行时依赖、无 AI 调用和 token 成本

下一步技术重点是**核心 1k 缺口排序、教学关系审核工作台和抽检**，而不是继续堆基础来源、调全景视觉或手改 `data.js`。

## 近期行动顺序

1. [x] 接入并审计 Démonette 2；1,675 条直接结构边已通过质量门并发布
2. [x] 接入并审计 DBnary；31,328 条法语定义与 2,720 条明示近义/反义已通过质量门并发布
3. [x] 纠正来源分工：CFDICT 相似不再影响位置，Démonette 不再独占可见关系，来源确认与人工审校在前端分层
4. 建立“核心 1k”优先级清单和官方覆盖仪表，优先找出仍无官方边、或只有 1 条基础边的高频词
5. 完成最小审核工作台，先处理能消灭官方孤词的 `compare / drift / trap / cause`，并为来源近义补不可互换条件
6. 以 200 条为一批，完成候选 → 审核 → 抽检 → 发布 → 回归闭环
7. 用真实 B1–C1 用户验证“逛图 → 聚焦 → 获得辨析洞察”，重点观察 candidate / sourced / reviewed / personal 是否易懂
8. 质量与成本均过关后，再扩至核心 3k 和全量 7,314

## 待验证 / 开放问题

- [x] v1 有效词表真实规模：7,314 个 eligible；后续允许因规则与审计迭代变化
- [ ] FLELex 的非商业许可是否符合产品长期方向，是否需要另建可商用词表
- [x] v1 多义词继续共用一个全景节点，详情按 DBnary 词条组展示义项；只有真实用户测试证明跨义项污染严重时才拆节点
- [ ] 哪种语义向量/语料最适合法语学习者视角，而不是一般语料相似度
- [x] v1 已用 mutual kNN + 325 条最小骨架实现 0 孤岛；骨架不参与词群角度
- [x] Démonette 直接结构边的 v1 自动发布规则已固定，并以来源哈希、主外键、方向冲突率和样例回归作为质量门
- [x] DBnary 义项与明示语义边的 v1 自动发布规则已固定；冲突词对暂缓，中文释义相似不再作为语义事实
- [ ] `compare` / `drift` / `trap` 的人工审核吞吐量和单位成本
- [x] v1 使用 Canvas + 固定坐标，7,371 节点 / 13,960 合并候选边可运行；仍需补移动端性能基线
- [ ] 用户是否把无标签全景理解为可探索地图，而不只是好看的散点背景
